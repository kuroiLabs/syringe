{
	"name": "@kuroi/syringe",
	"version": "3.0.0",
	"description": "TypeScript dependency injection framework",
	"main": "index.js",
	"scripts": {
		"build": "rm -Rf dist && tsc --project tsconfig.build.json && cp package.json dist/package.json && cp README.md dist/README.md",
		"pack": "npm pack ./dist",
		"predeploy": "npm run test && npm run build",
		"predeploy:test": "npm run test && npm run build",
		"deploy": "npm publish dist --access=public --scope=@kuroi",
		"deploy:test": "npm publish dist --access=public --scope=@kuroi --dry-run",
		"test": "vitest run"
	},
	"keywords": [
		"di",
		"dependency",
		"injection"
	],
	"author": "kuro <kuro@kuroi.io>",
	"license": "ISC",
	"devDependencies": {
		"shx": "^0.3.3",
		"ts-node": "^10.2.1",
		"typescript": "^5.8.3",
		"vitest": "^3.1.2"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/kuroiLabs/syringe.git"
	}
}
